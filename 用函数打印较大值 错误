#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int Max(int x, int y)
	{
		int x,y,z;
		if (x < y)
		{
			z = y;
		}
		else
			z = x;
		return z;
	}

    int a = 0;
	int b = 0;
	scanf("%d%d", &a, &b);
	int c = Max(a, b);
	printf("%d\n",c);

	return 0;

}
