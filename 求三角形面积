#include<math.h>
#include<stdio.h>

int main()
{
	double a, b, c, s, area;
	a = 3.25;
	b = 3.44;
	c = 2.38;
    s = (a + b + c) / 2;
	area = sqrt(s * (s - a) * (s - b) * (s - c));//sqrt为求平方根的函数
	printf("a=%tb=%tc=%t\n", a, b, c);//\t使输出位置跳到下一个Tab位置
	printf("area=%f\n", area);
	return 0;
}
