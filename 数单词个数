#define CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
int main()
{
	char str[100] = "final exam is coming so you cannot play your phone more";
	int count = 1;
	for (int i = 0; str[i] != '\0'; i++)
	{
		if (str[i] == ' ')
		{
			count++;
		}
	}
}
