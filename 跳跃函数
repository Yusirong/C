#define CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
        int x,y;
	scanf("%d",&x);
	if (x > 1)
	{
		y = 1;
	}
	else if (x < 1)
	{
		y = 0;
	}
	else if (x == 0)
	{
		y = 0;
	}
	printf("y=%d", y);
	return;
}
