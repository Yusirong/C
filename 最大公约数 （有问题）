#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int m = 0;
	int n = 0;
	int i = 0;
	scanf("%d%d", &m, &n);
	if (m < n)
	{
		int tmp = m;
		m = n;
		n = tmp;
	}
	if (m % n == 0)
	{
		printf("最大公约数为n ", n);
	}
	else
	{ 
		while (m % n == i)
		{
			m = n;
			n = i;
			if (m % n == 0)
			{
				printf("最大公约数为n", n);
				break;
			}
		}
	}
	return 0;
}
